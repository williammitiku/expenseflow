{
  "name": "@expenseflow/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, enums, constants, and utilities for ExpenseFlow",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"No shared tests yet\""
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
